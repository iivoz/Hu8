<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>موقع تواصل</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <!-- شاشة تسجيل الدخول -->
    <div class="login-container" id="login-container">
        <h2>تسجيل الدخول</h2>
        <div class="login-options">
            <button class="google-login" onclick="googleLogin()">تسجيل دخول باستخدام جوجل</button>
            <button class="manual-login" onclick="manualLogin()">تسجيل دخول يدوي</button>
        </div>
    </div>

    <!-- شاشة الدردشة -->
    <div class="chat-container" id="chat-container" style="display:none;">
        <div class="chat-header">
            <h3>أنت الآن في الدردشة</h3>
            <button onclick="logout()">تسجيل خروج</button>
        </div>
        <div class="chat-body">
            <div id="online-users">المستخدمون المتصلون:</div>
            <div id="messages"></div>
        </div>
        <div class="chat-footer">
            <input type="text" id="messageInput" placeholder="أكتب رسالة...">
            <input type="file" id="imageInput" onchange="sendImage()" />
            <button onclick="sendMessage()">إرسال</button>
        </div>
    </div>

    <!-- صفحة المجموعات -->
    <div class="groups-container" id="groups-container" style="display:none;">
        <h3>المجموعات</h3>
        <button onclick="createGroup()">إنشاء مجموعة</button>
        <div id="groupList"></div>
    </div>

    <!-- صفحة الدردشة الخاصة بالمجموعة -->
    <div class="group-chat-container" id="group-chat-container" style="display:none;">
        <div class="group-chat-header">
            <h3 id="groupName"></h3>
            <button onclick="exitGroup()">خروج من المجموعة</button>
        </div>
        <div class="group-chat-body" id="group-chat-body"></div>
        <div class="group-chat-footer">
            <input type="text" id="groupMessageInput" placeholder="أرسل رسالة إلى المجموعة...">
            <input type="file" id="groupImageInput" onchange="sendGroupImage()" />
            <button onclick="sendGroupMessage()">إرسال</button>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="app.js"></script>
</body>
</html>
